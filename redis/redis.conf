# Configuración base de Redis para AutoRent.
# La contraseña NO va aquí: la inyecta el entrypoint desde la variable
# de entorno REDIS_PASSWORD al arrancar (Plesk solo permite variables).

bind 0.0.0.0
protected-mode no
save 900 1
appendonly no